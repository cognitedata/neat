[cdf]
default_organization_dir = "scripts/integration_runner"
default_env = "dev"

[modules]
# This is the version of the modules. It should not be changed manually.
# It will be updated by the 'cdf module upgrade' command.
version = "0.4.10"

[alpha_flags]
classic = true


[plugins]
run = true
pull = false
dump = true
purge = true